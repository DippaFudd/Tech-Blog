<header class="header">
    <h1 class="site-title">Tech Blog</h1>
</header>

<section class="login-page card">
    <div class="container">
        <h2 class="section-title">Welcome</h2>
        <div class="form-container">
            <form class="login-form card">
                <h3 class="form-title">Login</h3>
                <label for="username-login">Username</label>
                <input type="text" id="username-login" name="username" class="input-text" required>

                <label for="password-login">Password</label>
                <input type="password" id="password-login" name="password" class="input-text" required>

                <button type="submit" class="btn btn-primary">Login</button> 
            </form>

            <form class="signup-form card">
                <h3 class="form-title">Sign Up</h3>
                <label for="username-signup">Username</label>
                <input type="text" id="username-signup" name="username" class="input-text" required>

                <label for="password-signup">Password</label>
                <input type="password" id="password-signup" name="password" class="input-text" required>

                <button type="submit" class="btn btn-primary">Sign Up</button> 
            </form>
        </div>
    </div>
</section>

<style>
    .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 2rem;
        background: #000000;
        color: white;
        border-radius: 10px;
        margin-bottom: 2rem;
    }
    
    .btn-home:hover {
        background-color: #0056b3;
    }
    .login-page {
        display: flex;
        flex-direction: column;
        align-items: center;
        background: linear-gradient(135deg, #000000, #007bff);
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .container {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        max-width: 800px;
        width: 100%;
        text-align: center;
    }
    .form-container {
        display: flex;
        gap: 2rem;
        justify-content: center;
    }
    .card {
        background: white;
        padding: 1.5rem;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        flex: 1 1 45%;
    }
    .form-title {
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: #333;
    }
    .input-text {
        margin-bottom: 1rem;
        padding: 0.5rem;
        border-radius: 5px;
        border: 1px solid #ccc;
        width: 100%;
    }
    .btn-primary {
        background-color: #007bff;
        color: white;
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 5px;
        cursor: pointer;
    }
    .btn-primary:hover {
        background-color: #0056b3;
    }
</style>

<script src="/js/login.js"></script>
<script src="/js/signup.js"></script>

